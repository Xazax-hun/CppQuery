List of tasks to accomplish:
	* If a matcher expression does not contains bind, automatically bind the top level bindable matcher's result.
	* Add doxygen documentation.
	* Use namespaces.
	* Format the whole project with clang format.
	* Persistent catalogue of Queries with descriptions.
	* Multithread parsing.
	* Context dependent autocompletition in Query language.
	* Do not execute queries with semantic errors, underline the wrong parts.
	* Serialize ASTs, and able to open projects that was already parsed earlier.