List of tasks to accomplish:
	* Drag and drop compilation databases.
	* Open search results in text editor.
	* Load search results from text files.
	* Filter search results based on path prefix.
	* Multithreaded parsing.
	* Context dependent autocompletition in Query language.
	* Do not execute queries with semantic errors, underline the wrong parts.
	* Serialize ASTs, and able to open projects that was already parsed earlier.
	* Add user guide. (Including how to make CMake to produce compilation database)
	* Add references to Bear as a tool to create compilation databases.
	* Remove using directives where it makes sense.
	* Do not try to execute the query if empty string is inside the query text.
	* Add screenshots to the Readme.md
	* "Incremential" reparse of projects.
